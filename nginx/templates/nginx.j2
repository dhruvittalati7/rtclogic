version: "3.7"
services:
  nginx:
    image: registry.rtclogic.com:1443/common/nginx:{{ nginx_version }}
    container_name: {{ container_name }}
    volumes:
    - {{ container_volume }}:/etc/nginx
    ports:
      - 443:443
      - 80:80
    restart: always

